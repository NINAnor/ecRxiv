# add files to ignore (do not push to GitHub) here.

*_cache
*_files/

# Editor (local VS Code / Cursor settings)
.vscode/

# Local datasets (e.g. NFI extracts for indicators such as NO_BGSK_001)
# Keep a short README in data/ if you want onboarding text tracked.
data/LSK_data_okologisk_tilstand_2026-06-03.csv
!data/README

# renv: commit renv.lock and renv/activate.R; omit the local library and caches
renv/library/
renv/local/
renv/cellar/
renv/lock/
renv/python/
renv/sandbox/
renv/staging/
# Nested renv library if Quarto/R is run from R/ (not the project root)
R/renv/

# macOS
.DS_Store

/.quarto/
**/*.quarto_ipynb
