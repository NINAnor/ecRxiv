# add files to ignore (not push to github) here.

*_cache
*_files/

# Editor / IDE
.vscode/

# Local LSK data deliveries (not for GitHub)
data/LSK_data_okologisk_tilstand*.csv

# renv: keep renv.lock and renv/activate.R; ignore local library and caches
renv/library/
renv/local/
renv/cellar/
renv/lock/
renv/python/
renv/sandbox/
renv/staging/
R/renv/
